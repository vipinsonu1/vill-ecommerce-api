"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var ClientMessage = require("../ClientMessage");
var BitsUtil_1 = require("../BitsUtil");
var MultiMapMessageType_1 = require("./MultiMapMessageType");
var REQUEST_TYPE = MultiMapMessageType_1.MultiMapMessageType.MULTIMAP_ISLOCKED;
var RESPONSE_TYPE = 101;
var RETRYABLE = true;
var MultiMapIsLockedCodec = (function () {
    function MultiMapIsLockedCodec() {
    }
    MultiMapIsLockedCodec.calculateSize = function (name, key) {
        var dataSize = 0;
        dataSize += BitsUtil_1.BitsUtil.calculateSizeString(name);
        dataSize += BitsUtil_1.BitsUtil.calculateSizeData(key);
        return dataSize;
    };
    MultiMapIsLockedCodec.encodeRequest = function (name, key) {
        var clientMessage = ClientMessage.newClientMessage(this.calculateSize(name, key));
        clientMessage.setMessageType(REQUEST_TYPE);
        clientMessage.setRetryable(RETRYABLE);
        clientMessage.appendString(name);
        clientMessage.appendData(key);
        clientMessage.updateFrameLength();
        return clientMessage;
    };
    MultiMapIsLockedCodec.decodeResponse = function (clientMessage, toObjectFunction) {
        if (toObjectFunction === void 0) { toObjectFunction = null; }
        var parameters = { 'response': null };
        parameters['response'] = clientMessage.readBoolean();
        return parameters;
    };
    return MultiMapIsLockedCodec;
}());
exports.MultiMapIsLockedCodec = MultiMapIsLockedCodec;
//# sourceMappingURL=MultiMapIsLockedCodec.js.map