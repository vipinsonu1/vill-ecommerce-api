"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var ClientMessage = require("../ClientMessage");
var BitsUtil_1 = require("../BitsUtil");
var RingbufferMessageType_1 = require("./RingbufferMessageType");
var REQUEST_TYPE = RingbufferMessageType_1.RingbufferMessageType.RINGBUFFER_ADD;
var RESPONSE_TYPE = 103;
var RETRYABLE = false;
var RingbufferAddCodec = (function () {
    function RingbufferAddCodec() {
    }
    RingbufferAddCodec.calculateSize = function (name, overflowPolicy, value) {
        var dataSize = 0;
        dataSize += BitsUtil_1.BitsUtil.calculateSizeString(name);
        dataSize += BitsUtil_1.BitsUtil.INT_SIZE_IN_BYTES;
        dataSize += BitsUtil_1.BitsUtil.calculateSizeData(value);
        return dataSize;
    };
    RingbufferAddCodec.encodeRequest = function (name, overflowPolicy, value) {
        var clientMessage = ClientMessage.newClientMessage(this.calculateSize(name, overflowPolicy, value));
        clientMessage.setMessageType(REQUEST_TYPE);
        clientMessage.setRetryable(RETRYABLE);
        clientMessage.appendString(name);
        clientMessage.appendInt32(overflowPolicy);
        clientMessage.appendData(value);
        clientMessage.updateFrameLength();
        return clientMessage;
    };
    RingbufferAddCodec.decodeResponse = function (clientMessage, toObjectFunction) {
        if (toObjectFunction === void 0) { toObjectFunction = null; }
        var parameters = { 'response': null };
        parameters['response'] = clientMessage.readLong();
        return parameters;
    };
    return RingbufferAddCodec;
}());
exports.RingbufferAddCodec = RingbufferAddCodec;
//# sourceMappingURL=RingbufferAddCodec.js.map