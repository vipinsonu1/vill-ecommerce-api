"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var ClientMessage = require("../ClientMessage");
var BitsUtil_1 = require("../BitsUtil");
var MapMessageType_1 = require("./MapMessageType");
var REQUEST_TYPE = MapMessageType_1.MapMessageType.MAP_VALUESWITHPAGINGPREDICATE;
var RESPONSE_TYPE = 117;
var RETRYABLE = true;
var MapValuesWithPagingPredicateCodec = (function () {
    function MapValuesWithPagingPredicateCodec() {
    }
    MapValuesWithPagingPredicateCodec.calculateSize = function (name, predicate) {
        var dataSize = 0;
        dataSize += BitsUtil_1.BitsUtil.calculateSizeString(name);
        dataSize += BitsUtil_1.BitsUtil.calculateSizeData(predicate);
        return dataSize;
    };
    MapValuesWithPagingPredicateCodec.encodeRequest = function (name, predicate) {
        var clientMessage = ClientMessage.newClientMessage(this.calculateSize(name, predicate));
        clientMessage.setMessageType(REQUEST_TYPE);
        clientMessage.setRetryable(RETRYABLE);
        clientMessage.appendString(name);
        clientMessage.appendData(predicate);
        clientMessage.updateFrameLength();
        return clientMessage;
    };
    MapValuesWithPagingPredicateCodec.decodeResponse = function (clientMessage, toObjectFunction) {
        if (toObjectFunction === void 0) { toObjectFunction = null; }
        var parameters = { 'response': null };
        var responseSize = clientMessage.readInt32();
        var response = [];
        for (var responseIndex = 0; responseIndex < responseSize; responseIndex++) {
            var responseItem;
            var responseItemKey;
            var responseItemVal;
            responseItemKey = clientMessage.readData();
            responseItemVal = clientMessage.readData();
            responseItem = [responseItemKey, responseItemVal];
            response.push(responseItem);
        }
        parameters['response'] = response;
        return parameters;
    };
    return MapValuesWithPagingPredicateCodec;
}());
exports.MapValuesWithPagingPredicateCodec = MapValuesWithPagingPredicateCodec;
//# sourceMappingURL=MapValuesWithPagingPredicateCodec.js.map