"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var ClientMessage = require("../ClientMessage");
var BitsUtil_1 = require("../BitsUtil");
var QueueMessageType_1 = require("./QueueMessageType");
var REQUEST_TYPE = QueueMessageType_1.QueueMessageType.QUEUE_REMOVELISTENER;
var RESPONSE_TYPE = 101;
var RETRYABLE = true;
var QueueRemoveListenerCodec = (function () {
    function QueueRemoveListenerCodec() {
    }
    QueueRemoveListenerCodec.calculateSize = function (name, registrationId) {
        var dataSize = 0;
        dataSize += BitsUtil_1.BitsUtil.calculateSizeString(name);
        dataSize += BitsUtil_1.BitsUtil.calculateSizeString(registrationId);
        return dataSize;
    };
    QueueRemoveListenerCodec.encodeRequest = function (name, registrationId) {
        var clientMessage = ClientMessage.newClientMessage(this.calculateSize(name, registrationId));
        clientMessage.setMessageType(REQUEST_TYPE);
        clientMessage.setRetryable(RETRYABLE);
        clientMessage.appendString(name);
        clientMessage.appendString(registrationId);
        clientMessage.updateFrameLength();
        return clientMessage;
    };
    QueueRemoveListenerCodec.decodeResponse = function (clientMessage, toObjectFunction) {
        if (toObjectFunction === void 0) { toObjectFunction = null; }
        var parameters = { 'response': null };
        parameters['response'] = clientMessage.readBoolean();
        return parameters;
    };
    return QueueRemoveListenerCodec;
}());
exports.QueueRemoveListenerCodec = QueueRemoveListenerCodec;
//# sourceMappingURL=QueueRemoveListenerCodec.js.map