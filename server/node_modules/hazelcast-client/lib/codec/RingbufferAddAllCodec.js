"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var ClientMessage = require("../ClientMessage");
var BitsUtil_1 = require("../BitsUtil");
var RingbufferMessageType_1 = require("./RingbufferMessageType");
var REQUEST_TYPE = RingbufferMessageType_1.RingbufferMessageType.RINGBUFFER_ADDALL;
var RESPONSE_TYPE = 103;
var RETRYABLE = false;
var RingbufferAddAllCodec = (function () {
    function RingbufferAddAllCodec() {
    }
    RingbufferAddAllCodec.calculateSize = function (name, valueList, overflowPolicy) {
        var dataSize = 0;
        dataSize += BitsUtil_1.BitsUtil.calculateSizeString(name);
        dataSize += BitsUtil_1.BitsUtil.INT_SIZE_IN_BYTES;
        valueList.forEach(function (valueListItem) {
            dataSize += BitsUtil_1.BitsUtil.calculateSizeData(valueListItem);
        });
        dataSize += BitsUtil_1.BitsUtil.INT_SIZE_IN_BYTES;
        return dataSize;
    };
    RingbufferAddAllCodec.encodeRequest = function (name, valueList, overflowPolicy) {
        var clientMessage = ClientMessage.newClientMessage(this.calculateSize(name, valueList, overflowPolicy));
        clientMessage.setMessageType(REQUEST_TYPE);
        clientMessage.setRetryable(RETRYABLE);
        clientMessage.appendString(name);
        clientMessage.appendInt32(valueList.length);
        valueList.forEach(function (valueListItem) {
            clientMessage.appendData(valueListItem);
        });
        clientMessage.appendInt32(overflowPolicy);
        clientMessage.updateFrameLength();
        return clientMessage;
    };
    RingbufferAddAllCodec.decodeResponse = function (clientMessage, toObjectFunction) {
        if (toObjectFunction === void 0) { toObjectFunction = null; }
        var parameters = { 'response': null };
        parameters['response'] = clientMessage.readLong();
        return parameters;
    };
    return RingbufferAddAllCodec;
}());
exports.RingbufferAddAllCodec = RingbufferAddAllCodec;
//# sourceMappingURL=RingbufferAddAllCodec.js.map