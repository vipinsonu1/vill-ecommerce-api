"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var ClientMessage = require("../ClientMessage");
var BitsUtil_1 = require("../BitsUtil");
var RingbufferMessageType_1 = require("./RingbufferMessageType");
var REQUEST_TYPE = RingbufferMessageType_1.RingbufferMessageType.RINGBUFFER_READONE;
var RESPONSE_TYPE = 105;
var RETRYABLE = true;
var RingbufferReadOneCodec = (function () {
    function RingbufferReadOneCodec() {
    }
    RingbufferReadOneCodec.calculateSize = function (name, sequence) {
        var dataSize = 0;
        dataSize += BitsUtil_1.BitsUtil.calculateSizeString(name);
        dataSize += BitsUtil_1.BitsUtil.LONG_SIZE_IN_BYTES;
        return dataSize;
    };
    RingbufferReadOneCodec.encodeRequest = function (name, sequence) {
        var clientMessage = ClientMessage.newClientMessage(this.calculateSize(name, sequence));
        clientMessage.setMessageType(REQUEST_TYPE);
        clientMessage.setRetryable(RETRYABLE);
        clientMessage.appendString(name);
        clientMessage.appendLong(sequence);
        clientMessage.updateFrameLength();
        return clientMessage;
    };
    RingbufferReadOneCodec.decodeResponse = function (clientMessage, toObjectFunction) {
        if (toObjectFunction === void 0) { toObjectFunction = null; }
        var parameters = { 'response': null };
        if (clientMessage.readBoolean() !== true) {
            parameters['response'] = toObjectFunction(clientMessage.readData());
        }
        return parameters;
    };
    return RingbufferReadOneCodec;
}());
exports.RingbufferReadOneCodec = RingbufferReadOneCodec;
//# sourceMappingURL=RingbufferReadOneCodec.js.map