"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var ClientMessage = require("../ClientMessage");
var BitsUtil_1 = require("../BitsUtil");
var SemaphoreMessageType_1 = require("./SemaphoreMessageType");
var REQUEST_TYPE = SemaphoreMessageType_1.SemaphoreMessageType.SEMAPHORE_DRAINPERMITS;
var RESPONSE_TYPE = 102;
var RETRYABLE = false;
var SemaphoreDrainPermitsCodec = (function () {
    function SemaphoreDrainPermitsCodec() {
    }
    SemaphoreDrainPermitsCodec.calculateSize = function (name) {
        var dataSize = 0;
        dataSize += BitsUtil_1.BitsUtil.calculateSizeString(name);
        return dataSize;
    };
    SemaphoreDrainPermitsCodec.encodeRequest = function (name) {
        var clientMessage = ClientMessage.newClientMessage(this.calculateSize(name));
        clientMessage.setMessageType(REQUEST_TYPE);
        clientMessage.setRetryable(RETRYABLE);
        clientMessage.appendString(name);
        clientMessage.updateFrameLength();
        return clientMessage;
    };
    SemaphoreDrainPermitsCodec.decodeResponse = function (clientMessage, toObjectFunction) {
        if (toObjectFunction === void 0) { toObjectFunction = null; }
        var parameters = { 'response': null };
        parameters['response'] = clientMessage.readInt32();
        return parameters;
    };
    return SemaphoreDrainPermitsCodec;
}());
exports.SemaphoreDrainPermitsCodec = SemaphoreDrainPermitsCodec;
//# sourceMappingURL=SemaphoreDrainPermitsCodec.js.map