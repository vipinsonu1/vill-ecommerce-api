"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var ClientMessage = require("../ClientMessage");
var BitsUtil_1 = require("../BitsUtil");
var ReplicatedMapMessageType_1 = require("./ReplicatedMapMessageType");
var REQUEST_TYPE = ReplicatedMapMessageType_1.ReplicatedMapMessageType.REPLICATEDMAP_CONTAINSKEY;
var RESPONSE_TYPE = 101;
var RETRYABLE = true;
var ReplicatedMapContainsKeyCodec = (function () {
    function ReplicatedMapContainsKeyCodec() {
    }
    ReplicatedMapContainsKeyCodec.calculateSize = function (name, key) {
        var dataSize = 0;
        dataSize += BitsUtil_1.BitsUtil.calculateSizeString(name);
        dataSize += BitsUtil_1.BitsUtil.calculateSizeData(key);
        return dataSize;
    };
    ReplicatedMapContainsKeyCodec.encodeRequest = function (name, key) {
        var clientMessage = ClientMessage.newClientMessage(this.calculateSize(name, key));
        clientMessage.setMessageType(REQUEST_TYPE);
        clientMessage.setRetryable(RETRYABLE);
        clientMessage.appendString(name);
        clientMessage.appendData(key);
        clientMessage.updateFrameLength();
        return clientMessage;
    };
    ReplicatedMapContainsKeyCodec.decodeResponse = function (clientMessage, toObjectFunction) {
        if (toObjectFunction === void 0) { toObjectFunction = null; }
        var parameters = { 'response': null };
        parameters['response'] = clientMessage.readBoolean();
        return parameters;
    };
    return ReplicatedMapContainsKeyCodec;
}());
exports.ReplicatedMapContainsKeyCodec = ReplicatedMapContainsKeyCodec;
//# sourceMappingURL=ReplicatedMapContainsKeyCodec.js.map