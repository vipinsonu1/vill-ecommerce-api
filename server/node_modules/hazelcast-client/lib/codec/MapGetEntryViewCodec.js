"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var ClientMessage = require("../ClientMessage");
var BitsUtil_1 = require("../BitsUtil");
var EntryViewCodec_1 = require("./EntryViewCodec");
var MapMessageType_1 = require("./MapMessageType");
var REQUEST_TYPE = MapMessageType_1.MapMessageType.MAP_GETENTRYVIEW;
var RESPONSE_TYPE = 111;
var RETRYABLE = true;
var MapGetEntryViewCodec = (function () {
    function MapGetEntryViewCodec() {
    }
    MapGetEntryViewCodec.calculateSize = function (name, key, threadId) {
        var dataSize = 0;
        dataSize += BitsUtil_1.BitsUtil.calculateSizeString(name);
        dataSize += BitsUtil_1.BitsUtil.calculateSizeData(key);
        dataSize += BitsUtil_1.BitsUtil.LONG_SIZE_IN_BYTES;
        return dataSize;
    };
    MapGetEntryViewCodec.encodeRequest = function (name, key, threadId) {
        var clientMessage = ClientMessage.newClientMessage(this.calculateSize(name, key, threadId));
        clientMessage.setMessageType(REQUEST_TYPE);
        clientMessage.setRetryable(RETRYABLE);
        clientMessage.appendString(name);
        clientMessage.appendData(key);
        clientMessage.appendLong(threadId);
        clientMessage.updateFrameLength();
        return clientMessage;
    };
    MapGetEntryViewCodec.decodeResponse = function (clientMessage, toObjectFunction) {
        if (toObjectFunction === void 0) { toObjectFunction = null; }
        var parameters = { 'response': null };
        if (clientMessage.readBoolean() !== true) {
            parameters['response'] = EntryViewCodec_1.EntryViewCodec.decode(clientMessage, toObjectFunction);
        }
        return parameters;
    };
    return MapGetEntryViewCodec;
}());
exports.MapGetEntryViewCodec = MapGetEntryViewCodec;
//# sourceMappingURL=MapGetEntryViewCodec.js.map