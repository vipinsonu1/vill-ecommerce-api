"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var ClientMessage = require("../ClientMessage");
var BitsUtil_1 = require("../BitsUtil");
var QueueMessageType_1 = require("./QueueMessageType");
var REQUEST_TYPE = QueueMessageType_1.QueueMessageType.QUEUE_DRAINTOMAXSIZE;
var RESPONSE_TYPE = 106;
var RETRYABLE = false;
var QueueDrainToMaxSizeCodec = (function () {
    function QueueDrainToMaxSizeCodec() {
    }
    QueueDrainToMaxSizeCodec.calculateSize = function (name, maxSize) {
        var dataSize = 0;
        dataSize += BitsUtil_1.BitsUtil.calculateSizeString(name);
        dataSize += BitsUtil_1.BitsUtil.INT_SIZE_IN_BYTES;
        return dataSize;
    };
    QueueDrainToMaxSizeCodec.encodeRequest = function (name, maxSize) {
        var clientMessage = ClientMessage.newClientMessage(this.calculateSize(name, maxSize));
        clientMessage.setMessageType(REQUEST_TYPE);
        clientMessage.setRetryable(RETRYABLE);
        clientMessage.appendString(name);
        clientMessage.appendInt32(maxSize);
        clientMessage.updateFrameLength();
        return clientMessage;
    };
    QueueDrainToMaxSizeCodec.decodeResponse = function (clientMessage, toObjectFunction) {
        if (toObjectFunction === void 0) { toObjectFunction = null; }
        var parameters = { 'response': null };
        var responseSize = clientMessage.readInt32();
        var response = [];
        for (var responseIndex = 0; responseIndex < responseSize; responseIndex++) {
            var responseItem;
            responseItem = clientMessage.readData();
            response.push(responseItem);
        }
        parameters['response'] = response;
        return parameters;
    };
    return QueueDrainToMaxSizeCodec;
}());
exports.QueueDrainToMaxSizeCodec = QueueDrainToMaxSizeCodec;
//# sourceMappingURL=QueueDrainToMaxSizeCodec.js.map