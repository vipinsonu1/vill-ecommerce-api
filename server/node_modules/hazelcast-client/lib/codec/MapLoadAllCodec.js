"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var ClientMessage = require("../ClientMessage");
var BitsUtil_1 = require("../BitsUtil");
var MapMessageType_1 = require("./MapMessageType");
var REQUEST_TYPE = MapMessageType_1.MapMessageType.MAP_LOADALL;
var RESPONSE_TYPE = 100;
var RETRYABLE = false;
var MapLoadAllCodec = (function () {
    function MapLoadAllCodec() {
    }
    MapLoadAllCodec.calculateSize = function (name, replaceExistingValues) {
        var dataSize = 0;
        dataSize += BitsUtil_1.BitsUtil.calculateSizeString(name);
        dataSize += BitsUtil_1.BitsUtil.BOOLEAN_SIZE_IN_BYTES;
        return dataSize;
    };
    MapLoadAllCodec.encodeRequest = function (name, replaceExistingValues) {
        var clientMessage = ClientMessage.newClientMessage(this.calculateSize(name, replaceExistingValues));
        clientMessage.setMessageType(REQUEST_TYPE);
        clientMessage.setRetryable(RETRYABLE);
        clientMessage.appendString(name);
        clientMessage.appendBoolean(replaceExistingValues);
        clientMessage.updateFrameLength();
        return clientMessage;
    };
    return MapLoadAllCodec;
}());
exports.MapLoadAllCodec = MapLoadAllCodec;
//# sourceMappingURL=MapLoadAllCodec.js.map