"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var ClientMessage = require("../ClientMessage");
var BitsUtil_1 = require("../BitsUtil");
var MapMessageType_1 = require("./MapMessageType");
var REQUEST_TYPE = MapMessageType_1.MapMessageType.MAP_VALUESWITHPREDICATE;
var RESPONSE_TYPE = 106;
var RETRYABLE = true;
var MapValuesWithPredicateCodec = (function () {
    function MapValuesWithPredicateCodec() {
    }
    MapValuesWithPredicateCodec.calculateSize = function (name, predicate) {
        var dataSize = 0;
        dataSize += BitsUtil_1.BitsUtil.calculateSizeString(name);
        dataSize += BitsUtil_1.BitsUtil.calculateSizeData(predicate);
        return dataSize;
    };
    MapValuesWithPredicateCodec.encodeRequest = function (name, predicate) {
        var clientMessage = ClientMessage.newClientMessage(this.calculateSize(name, predicate));
        clientMessage.setMessageType(REQUEST_TYPE);
        clientMessage.setRetryable(RETRYABLE);
        clientMessage.appendString(name);
        clientMessage.appendData(predicate);
        clientMessage.updateFrameLength();
        return clientMessage;
    };
    MapValuesWithPredicateCodec.decodeResponse = function (clientMessage, toObjectFunction) {
        if (toObjectFunction === void 0) { toObjectFunction = null; }
        var parameters = { 'response': null };
        var responseSize = clientMessage.readInt32();
        var response = [];
        for (var responseIndex = 0; responseIndex < responseSize; responseIndex++) {
            var responseItem;
            responseItem = clientMessage.readData();
            response.push(responseItem);
        }
        parameters['response'] = response;
        return parameters;
    };
    return MapValuesWithPredicateCodec;
}());
exports.MapValuesWithPredicateCodec = MapValuesWithPredicateCodec;
//# sourceMappingURL=MapValuesWithPredicateCodec.js.map