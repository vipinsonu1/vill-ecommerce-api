"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var ClientMessage = require("../ClientMessage");
var BitsUtil_1 = require("../BitsUtil");
var MapMessageType_1 = require("./MapMessageType");
var REQUEST_TYPE = MapMessageType_1.MapMessageType.MAP_REPLACEIFSAME;
var RESPONSE_TYPE = 101;
var RETRYABLE = false;
var MapReplaceIfSameCodec = (function () {
    function MapReplaceIfSameCodec() {
    }
    MapReplaceIfSameCodec.calculateSize = function (name, key, testValue, value, threadId) {
        var dataSize = 0;
        dataSize += BitsUtil_1.BitsUtil.calculateSizeString(name);
        dataSize += BitsUtil_1.BitsUtil.calculateSizeData(key);
        dataSize += BitsUtil_1.BitsUtil.calculateSizeData(testValue);
        dataSize += BitsUtil_1.BitsUtil.calculateSizeData(value);
        dataSize += BitsUtil_1.BitsUtil.LONG_SIZE_IN_BYTES;
        return dataSize;
    };
    MapReplaceIfSameCodec.encodeRequest = function (name, key, testValue, value, threadId) {
        var clientMessage = ClientMessage.newClientMessage(this.calculateSize(name, key, testValue, value, threadId));
        clientMessage.setMessageType(REQUEST_TYPE);
        clientMessage.setRetryable(RETRYABLE);
        clientMessage.appendString(name);
        clientMessage.appendData(key);
        clientMessage.appendData(testValue);
        clientMessage.appendData(value);
        clientMessage.appendLong(threadId);
        clientMessage.updateFrameLength();
        return clientMessage;
    };
    MapReplaceIfSameCodec.decodeResponse = function (clientMessage, toObjectFunction) {
        if (toObjectFunction === void 0) { toObjectFunction = null; }
        var parameters = { 'response': null };
        parameters['response'] = clientMessage.readBoolean();
        return parameters;
    };
    return MapReplaceIfSameCodec;
}());
exports.MapReplaceIfSameCodec = MapReplaceIfSameCodec;
//# sourceMappingURL=MapReplaceIfSameCodec.js.map