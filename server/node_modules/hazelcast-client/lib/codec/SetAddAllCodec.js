"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var ClientMessage = require("../ClientMessage");
var BitsUtil_1 = require("../BitsUtil");
var SetMessageType_1 = require("./SetMessageType");
var REQUEST_TYPE = SetMessageType_1.SetMessageType.SET_ADDALL;
var RESPONSE_TYPE = 101;
var RETRYABLE = false;
var SetAddAllCodec = (function () {
    function SetAddAllCodec() {
    }
    SetAddAllCodec.calculateSize = function (name, valueList) {
        var dataSize = 0;
        dataSize += BitsUtil_1.BitsUtil.calculateSizeString(name);
        dataSize += BitsUtil_1.BitsUtil.INT_SIZE_IN_BYTES;
        valueList.forEach(function (valueListItem) {
            dataSize += BitsUtil_1.BitsUtil.calculateSizeData(valueListItem);
        });
        return dataSize;
    };
    SetAddAllCodec.encodeRequest = function (name, valueList) {
        var clientMessage = ClientMessage.newClientMessage(this.calculateSize(name, valueList));
        clientMessage.setMessageType(REQUEST_TYPE);
        clientMessage.setRetryable(RETRYABLE);
        clientMessage.appendString(name);
        clientMessage.appendInt32(valueList.length);
        valueList.forEach(function (valueListItem) {
            clientMessage.appendData(valueListItem);
        });
        clientMessage.updateFrameLength();
        return clientMessage;
    };
    SetAddAllCodec.decodeResponse = function (clientMessage, toObjectFunction) {
        if (toObjectFunction === void 0) { toObjectFunction = null; }
        var parameters = { 'response': null };
        parameters['response'] = clientMessage.readBoolean();
        return parameters;
    };
    return SetAddAllCodec;
}());
exports.SetAddAllCodec = SetAddAllCodec;
//# sourceMappingURL=SetAddAllCodec.js.map