"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var ClientMessage = require("../ClientMessage");
var BitsUtil_1 = require("../BitsUtil");
var SetMessageType_1 = require("./SetMessageType");
var REQUEST_TYPE = SetMessageType_1.SetMessageType.SET_REMOVELISTENER;
var RESPONSE_TYPE = 101;
var RETRYABLE = true;
var SetRemoveListenerCodec = (function () {
    function SetRemoveListenerCodec() {
    }
    SetRemoveListenerCodec.calculateSize = function (name, registrationId) {
        var dataSize = 0;
        dataSize += BitsUtil_1.BitsUtil.calculateSizeString(name);
        dataSize += BitsUtil_1.BitsUtil.calculateSizeString(registrationId);
        return dataSize;
    };
    SetRemoveListenerCodec.encodeRequest = function (name, registrationId) {
        var clientMessage = ClientMessage.newClientMessage(this.calculateSize(name, registrationId));
        clientMessage.setMessageType(REQUEST_TYPE);
        clientMessage.setRetryable(RETRYABLE);
        clientMessage.appendString(name);
        clientMessage.appendString(registrationId);
        clientMessage.updateFrameLength();
        return clientMessage;
    };
    SetRemoveListenerCodec.decodeResponse = function (clientMessage, toObjectFunction) {
        if (toObjectFunction === void 0) { toObjectFunction = null; }
        var parameters = { 'response': null };
        parameters['response'] = clientMessage.readBoolean();
        return parameters;
    };
    return SetRemoveListenerCodec;
}());
exports.SetRemoveListenerCodec = SetRemoveListenerCodec;
//# sourceMappingURL=SetRemoveListenerCodec.js.map