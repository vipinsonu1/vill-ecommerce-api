"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var ClientMessage = require("../ClientMessage");
var BitsUtil_1 = require("../BitsUtil");
var MapMessageType_1 = require("./MapMessageType");
var REQUEST_TYPE = MapMessageType_1.MapMessageType.MAP_LOADGIVENKEYS;
var RESPONSE_TYPE = 100;
var RETRYABLE = false;
var MapLoadGivenKeysCodec = (function () {
    function MapLoadGivenKeysCodec() {
    }
    MapLoadGivenKeysCodec.calculateSize = function (name, keys, replaceExistingValues) {
        var dataSize = 0;
        dataSize += BitsUtil_1.BitsUtil.calculateSizeString(name);
        dataSize += BitsUtil_1.BitsUtil.INT_SIZE_IN_BYTES;
        keys.forEach(function (keysItem) {
            dataSize += BitsUtil_1.BitsUtil.calculateSizeData(keysItem);
        });
        dataSize += BitsUtil_1.BitsUtil.BOOLEAN_SIZE_IN_BYTES;
        return dataSize;
    };
    MapLoadGivenKeysCodec.encodeRequest = function (name, keys, replaceExistingValues) {
        var clientMessage = ClientMessage.newClientMessage(this.calculateSize(name, keys, replaceExistingValues));
        clientMessage.setMessageType(REQUEST_TYPE);
        clientMessage.setRetryable(RETRYABLE);
        clientMessage.appendString(name);
        clientMessage.appendInt32(keys.length);
        keys.forEach(function (keysItem) {
            clientMessage.appendData(keysItem);
        });
        clientMessage.appendBoolean(replaceExistingValues);
        clientMessage.updateFrameLength();
        return clientMessage;
    };
    return MapLoadGivenKeysCodec;
}());
exports.MapLoadGivenKeysCodec = MapLoadGivenKeysCodec;
//# sourceMappingURL=MapLoadGivenKeysCodec.js.map