"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var ClientMessage = require("../ClientMessage");
var BitsUtil_1 = require("../BitsUtil");
var MapMessageType_1 = require("./MapMessageType");
var REQUEST_TYPE = MapMessageType_1.MapMessageType.MAP_REMOVEENTRYLISTENER;
var RESPONSE_TYPE = 101;
var RETRYABLE = true;
var MapRemoveEntryListenerCodec = (function () {
    function MapRemoveEntryListenerCodec() {
    }
    MapRemoveEntryListenerCodec.calculateSize = function (name, registrationId) {
        var dataSize = 0;
        dataSize += BitsUtil_1.BitsUtil.calculateSizeString(name);
        dataSize += BitsUtil_1.BitsUtil.calculateSizeString(registrationId);
        return dataSize;
    };
    MapRemoveEntryListenerCodec.encodeRequest = function (name, registrationId) {
        var clientMessage = ClientMessage.newClientMessage(this.calculateSize(name, registrationId));
        clientMessage.setMessageType(REQUEST_TYPE);
        clientMessage.setRetryable(RETRYABLE);
        clientMessage.appendString(name);
        clientMessage.appendString(registrationId);
        clientMessage.updateFrameLength();
        return clientMessage;
    };
    MapRemoveEntryListenerCodec.decodeResponse = function (clientMessage, toObjectFunction) {
        if (toObjectFunction === void 0) { toObjectFunction = null; }
        var parameters = { 'response': null };
        parameters['response'] = clientMessage.readBoolean();
        return parameters;
    };
    return MapRemoveEntryListenerCodec;
}());
exports.MapRemoveEntryListenerCodec = MapRemoveEntryListenerCodec;
//# sourceMappingURL=MapRemoveEntryListenerCodec.js.map